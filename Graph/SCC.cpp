// code of finding strong connected component with DFS

vector<vector<int>> scc(vector<vector<int>> &graph) {
    int noofnode = graph.size();
    vector<int> tOrder(noofnode, -1);
    int index(0);
    
}
